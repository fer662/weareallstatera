<% if (!user) { %>
  <p>Welcome! Please <a href="/login">log in</a>.</p>
<% } else { %>
  <p>Hello, <%= user.name %>. <a href="/logout">logout</a>.</p>
  <p>You have <%= pendingTweets %> pending tweets to like</p><a href="/like">Like them now</a>
<% } %>
<p>Liking #WeAreAllStatera with the force of <%= twitterClients %> users</p>

