<% if (!user) { %>
  <p>Welcome! Please <a href="/login">log in</a>.</p>
<% } else { %>
  <p>Hello, <%= user.twitterId %>. <a href="/logout">logout</a>.</p>
<% } %>
<p>Liking #WeAreAllStatera with the force of <%= twitterClients.length %> users</p>